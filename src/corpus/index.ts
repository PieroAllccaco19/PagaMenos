// PagaMenos · src/corpus — version-controlled corpus data, schema & seed builder
// (M0 boundary placeholder). Pure like the engine: MUST NOT import db / app / analytics /
// sourcemon / services, Next, React, Prisma, or perform I/O. It may depend on the engine's
// pure types and on Zod. Corpus v1 (46 rules) is encoded in M1 — not during M0.
export {};
