// PagaMenos · src/analytics — first-party canonical_event writer + export queries
// (M0 boundary placeholder). Telemetry only; never the source of truth for domain facts.
// No events are implemented during M0.
export {};
